#!/bin/bash
# ==============================================================================
# EFD macOS 一鍵安裝與啟動入口 (在 macOS Finder 雙擊此檔即可執行)
# ==============================================================================
DIR="$(cd "$(dirname "$0")" && pwd)"
bash "${DIR}/macos/Install-EFD.command"

